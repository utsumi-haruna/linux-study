# Linuxコマンド

## よく使うコマンド

### シャットダウン

- `sudo shutdown -h now`
- `-h` は「halt(ホルト)」の略で、停止という意味。
- `-r` にすると再起動出来る。

### 再起動

- `sudo reboot`

### ipアドレスの確認

- `ip a`

## ファイル・ディレクトリ関連

### カレントディレクトリを表示

- `pwd`

### ディレクトリ内のファイル・ディレクトリを一覧表示

- `ls`：通常表示
- `ls -a`：隠しファイル表示
- `ls -l`：1行ずつ詳細情報表示
- `ls -al`：全てのファイルの詳細を表示

### ディレクトリ移動

- `cd`

### ファイルの作成

- `touch <ファイル名>`

### フォルダの作成

- `mkdir <フォルダ名>`
- `mkdir -p /web/kad01`：親ディレクトリごと作成

### ファイル・ディレクトリをコピー

- `cp <コピーするファイル><コピー先ディレクトリ>`
- `cp -r`：ディレクトリごとコピー
- コピー先に新しいファイル名を書けば、リネームしてコピーできる。

### 移動・リネーム

- 移動：`mv <移動させるもの><移動先>`
- リネーム：`mv <名前を変えたいもの><変えたい名前>`

### 削除

- `rm <ファイル名>`
- `rm -f`：強制削除
- `rm -r`：ディレクトリごと削除
- ディレクトリ削除の際確認が面倒な為、`-r -f` または `-rf` をよく使う。

### ファイルの内容を表示

- `cat <ファイル名>`

## ユーザー・グループ関連

### ユーザー/グループの追加

- `sudo adduser <ユーザー>`
- `sudo addgroup <グループ名>`

### ユーザー/グループの削除

- `sudo deluser <ユーザー名>`
- `sudo delgroup <グループ名>`

### パスワードの変更

- `sudo passwd <ユーザー名/グループ名>`

### ユーザーの変更

- `su <ユーザー名>`

## ソフトウェア、パッケージ関連

### aptコマンド

---

`sudo apt <サブコマンド>`

#### サブコマンド一覧

| コマンド             | 用途                                         |
| -------------------- | -------------------------------------------- |
| `install <ソフト名>` | ソフトウェアのインストール                   |
| `remove <ソフト名>`  | アンインストール                             |
| `update`             | リポジトリ(ソフトウェアの倉庫)のアップデート |
| `upgrade`            | ソフトウェアの一括更新                       |

- `-y` を最後に付ければ自動的にyesとして実行する

### systemctlコマンド

---

`sudo systemctl <サブコマンド> <ソフト名>`

#### サブコマンド一覧

| コマンド              | 用途                  |
| --------------------- | --------------------- |
| `status`              | 状態確認              |
| `start` / `stop`      | 起動 / 停止           |
| `restart` / `reload`  | 再起動 / 再読み込み   |
| `enable ` / `disable` | 自動起動のオン / オフ |
| `is-enabled`          | 自動起動の確認        |

## vim(テキストエディタ)

| コマンド                | 用途                       |
| ----------------------- | -------------------------- |
| `sudu vim <ファイル名>` | vimを使用                  |
| `i`                     | インサートモードに切り替え |
| `esc`                   | コマンドモードに切り替え   |
| `:wq`                   | 保存して終了               |
| `:q`                    | 保存せず終了               |
